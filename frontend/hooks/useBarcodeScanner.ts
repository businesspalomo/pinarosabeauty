'use client';
import { useEffect, useRef } from 'react';
export function useBarcodeScanner(onScan:(code:string)=>void,enabled=true){const buffer=useRef('');const last=useRef(0);const cb=useRef(onScan);cb.current=onScan;useEffect(()=>{if(!enabled)return;const handler=(e:KeyboardEvent)=>{const target=e.target as HTMLElement|null;if(target && ['INPUT','TEXTAREA','SELECT'].includes(target.tagName) && !(target as HTMLInputElement).dataset.scanner) return;const now=Date.now();if(now-last.current>120)buffer.current='';last.current=now;if(e.key==='Enter'){const code=buffer.current.trim();buffer.current='';if(code.length>=4){e.preventDefault();cb.current(code);}return;}if(e.key.length===1&&!e.ctrlKey&&!e.metaKey&&!e.altKey) buffer.current+=e.key;};window.addEventListener('keydown',handler,true);return()=>window.removeEventListener('keydown',handler,true)},[enabled]);}

'use client';
import Link from 'next/link'; import { usePathname, useRouter } from 'next/navigation'; import { Boxes, LayoutDashboard, PackagePlus, Users, ClipboardList, ScanLine, ArrowLeftRight, Store, ShieldCheck, LogOut } from 'lucide-react'; import { supabase } from '@/lib/supabaseClient';
const items=[['/','Inicio',LayoutDashboard],['/productos','Productos',Boxes],['/recepcion','Recepción',PackagePlus],['/clientes','Clientes',Users],['/pedidos','Pedidos',ClipboardList],['/picking','Picking',ScanLine],['/movimientos','Movimientos',ArrowLeftRight],['/catalogo','Catálogo',Store],['/usuarios','Usuarios',ShieldCheck]] as const;
export function Sidebar(){const p=usePathname();const router=useRouter();async function logout(){await supabase.auth.signOut();router.replace('/login');}return <aside className="sidebar"><div className="brand"><div className="brandMark">PR</div><div><strong>Piña Rosa</strong><span>Inventory OS</span></div></div><nav>{items.map(([href,label,Icon])=><Link key={href} href={href} className={(href==='/'?p===href:p.startsWith(href))?'active':''}><Icon size={18}/><span>{label}</span></Link>)}</nav><div className="sideFooter"><button className="btn ghost" style={{width:'100%',justifyContent:'center'}} onClick={logout}><LogOut size={16}/><span>Cerrar sesión</span></button></div></aside>}

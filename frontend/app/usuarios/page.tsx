'use client';import { useEffect,useState } from 'react';import { Page } from '@/components/Page';import { api } from '@/lib/api';
export default function Users(){const[r,setR]=useState<any[]>([]);useEffect(()=>{api('/users').then(setR).catch(()=>{})},[]);return <Page title="Usuarios" subtitle="Durante desarrollo usamos un administrador local. Luego se reemplaza por Supabase Auth."><section className="panel"><div className="tableWrap"><table><thead><tr><th>Nombre</th><th>Email</th><th>Rol</th><th>Estado</th></tr></thead><tbody>{r.map(u=><tr key={u.id}><td><strong>{u.name}</strong></td><td>{u.email}</td><td><span className="badge">{u.role}</span></td><td><span className={`badge ${u.active?'ok':'gray'}`}>{u.active?'Activo':'Inactivo'}</span></td></tr>)}</tbody></table></div></section></Page>}

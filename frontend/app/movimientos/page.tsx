'use client';import { useEffect,useState } from 'react';import { Page } from '@/components/Page';import { api } from '@/lib/api';
export default function Movements(){const[r,setR]=useState<any[]>([]),[e,setE]=useState('');useEffect(()=>{api('/movements').then(setR).catch(x=>setE(x.message))},[]);return <Page title="Movimientos" subtitle="Trazabilidad: toda entrada, reserva y salida queda registrada."><section className="panel">{e&&<div className="notice error">{e}</div>}<div className="tableWrap"><table><thead><tr><th>Fecha</th><th>Producto</th><th>Tipo</th><th>Cantidad</th><th>Físico</th><th>Reservado</th><th>Ubicación</th><th>Usuario</th></tr></thead><tbody>{r.map(m=><tr key={m.id}><td>{new Date(m.created_at).toLocaleString('es-AR')}</td><td><strong>{m.brand} {m.product}</strong><div className="muted">{m.shade||'—'} · {m.sku}</div></td><td><span className="badge">{m.movement_type}</span></td><td>{m.quantity>0?'+':''}{m.quantity}</td><td>{m.stock_before} → {m.stock_after}</td><td>{m.reserved_before} → {m.reserved_after}</td><td>{m.location}</td><td>{m.user_name||'Sistema'}</td></tr>)}</tbody></table></div></section></Page>}

import { Body, Controller, Get, Param, Post } from '@nestjs/common'; import { CoreService } from './core.service'; import { CurrentUserId } from './current-user';
@Controller('receiving') export class ReceivingController{constructor(private s:CoreService){} @Post() start(@Body() b:any,@CurrentUserId() u:string){return this.s.startReceiving(b,u)} @Get(':id') get(@Param('id') id:string){return this.s.receivingProgress(id)} @Post(':id/scan') scan(@Param('id') id:string,@Body() b:any,@CurrentUserId() u:string){return this.s.receiveScan(id,String(b.barcode||''),String(b.locationCode||'RECEPCION'),u)} @Post(':id/complete') complete(@Param('id') id:string,@CurrentUserId() u:string){return this.s.completeReceiving(id,u)}}

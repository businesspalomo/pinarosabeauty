import { Body, Controller, Get, Param, Post } from '@nestjs/common'; import { CoreService } from './core.service'; import { CurrentUserId } from './current-user';
@Controller('orders') export class OrdersController{constructor(private s:CoreService){} @Get() list(){return this.s.listOrders()} @Get(':id') get(@Param('id') id:string){return this.s.getOrder(id)} @Post() create(@Body() b:any,@CurrentUserId() u:string){return this.s.createOrder(b,u)} @Post(':id/confirm') confirm(@Param('id') id:string,@CurrentUserId() u:string){return this.s.confirmOrder(id,u)} @Post(':id/dispatch') dispatch(@Param('id') id:string,@CurrentUserId() u:string){return this.s.dispatchOrder(id,u)}}

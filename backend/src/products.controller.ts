import { Body, Controller, Delete, Get, Param, Patch, Post, Query } from '@nestjs/common'; import { CoreService } from './core.service'; import { CurrentUserId } from './current-user';
@Controller('products') export class ProductsController{constructor(private s:CoreService){} @Get() list(@Query('q') q=''){return this.s.listProducts(q)} @Get('shade-suggestions') shades(@Query('brand') b='',@Query('product') p=''){return this.s.shadeSuggestions(b,p)} @Post() create(@Body() b:any,@CurrentUserId() u:string){return this.s.createProduct(b,u)} @Patch(':id') update(@Param('id') id:string,@Body() b:any,@CurrentUserId() u:string){return this.s.updateProduct(id,b,u)} @Delete(':id') remove(@Param('id') id:string,@CurrentUserId() u:string){return this.s.deleteProduct(id,u)}}

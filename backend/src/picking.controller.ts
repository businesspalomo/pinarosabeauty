import { Body, Controller, Get, Param, Post } from '@nestjs/common'; import { CoreService } from './core.service'; import { CurrentUserId } from './current-user';
@Controller('picking') export class PickingController{constructor(private s:CoreService){} @Post(':orderId/start') start(@Param('orderId') id:string,@CurrentUserId() u:string){return this.s.startPicking(id,u)} @Get(':orderId') get(@Param('orderId') id:string){return this.s.pickingProgress(id)} @Post(':orderId/scan') scan(@Param('orderId') id:string,@Body() b:any,@CurrentUserId() u:string){return this.s.pickScan(id,String(b.barcode||''),u)}}
